# Why React?
- We can use states which means that once we update th state variables, it changes across the page
- we can split our app into multiple component and reuse those components
- React uses a virtual DOM to efficiently update the UI which is better than updating content using DOM Manipulation
- Debugging and Maintainance is easy