// Advantage of add js file sperate
// 1.) Improved maintainability: Keeping JavaScript code in separate files makes it easier to manage and update without affecting the HTML structure.
// 2.) Enhanced reusability: Separate JavaScript files can be reused across multiple HTML pages, reducing code duplication and improving consistency.
// 3.) Browser caching: When JavaScript files are cached by the browser, subsequent page loads can be faster since the file doesn't need to be downloaded again.

// alert("Hello, World!");
// console.log("This is a message in the console.");

// Window Object:-
// The window object represents an open window in a browser. It is browser's object (not Javascript's) & is automatically created by browser.
// It is a global with lots of properties and methods.
window.console.log("Hello, World!");
// NOTE: No need to write window because JS knows that console is a property of window object.